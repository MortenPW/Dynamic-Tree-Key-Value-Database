readme of tree

Tests the various implementations of tree.
See tree.h (under inc) for implementation details.

Compile and run:
	
	1. Open terminal and navigate to directory			(terminal/dir)
	2. Run make to compile the program 					(make)
	3. Go to bin/ 										(cd bin)
	4. Execute file by issuing command ./tree			(./tree)
